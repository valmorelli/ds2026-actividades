const nombre = 'Valentina';
let edad = 20;
const materia = 'Desarrollo de software';

console.log (`Me llamo ${nombre}, tengo ${edad} años y curso ${materia}`);

let cont = 0;

for (let i = 0; i < 3; i++ ) {
      cont++;
      console.log (`contador en: ${cont}`); 
}

console.log (`valor final del contador: ${cont}`);
